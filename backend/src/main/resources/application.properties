
# Application
spring.application.name=campus-exchange

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/campus_exchange
spring.datasource.username=root
spring.datasource.password=Srushti@2022
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate automatically detects the dialect.
# No need to specify spring.jpa.database-platform


# Server
server.port=8080

# Logging 
logging.level.org.springframework.security=DEBUG